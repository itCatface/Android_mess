<?xml version="1.0" encoding="utf-8"?>
<cc.catface.clibrary.util.view.recyclerview.swipe.SwipeItemLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/swipe_layout"
    android:layout_width="match_parent"
    android:layout_height="@dimen/swipe_item_height">


    <LinearLayout
        android:layout_width="180dp"
        android:layout_height="match_parent"
        android:layout_gravity="left"
        android:gravity="center"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/left_1"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="#f00"
            android:gravity="center"
            android:text="置顶"/>

        <TextView
            android:id="@+id/left_2"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="#0f0"
            android:gravity="center"
            android:text="点我试试看"/>

    </LinearLayout>

    <!--<TextView
        android:id="@left_1menu"
        android:layout_width="@dimen/swipe_item_menu_width"
        android:layout_height="match_parent"
        android:layout_gravity="left"
        android:background="@color/red500"
        android:gravity="center"
        android:text="置顶"
        android:textColor="@color/white"/>-->

    <TextView
        android:id="@+id/right_menu"
        android:layout_width="@dimen/swipe_item_menu_width"
        android:layout_height="match_parent"
        android:layout_gravity="right"
        android:background="@color/blue500"
        android:gravity="center"
        android:text="删除"
        android:textColor="@color/white"/>

    <include layout="@layout/item_note"/>

</cc.catface.clibrary.util.view.recyclerview.swipe.SwipeItemLayout>